# Delivery Service — Test Task (Citadello)

Независимый микросервис `delivery`, предназначенный для расчёта стоимости доставки от внешних служб (СДЭК, Boxberry и др.).  
Реализован на **NestJS**, построен по принципам **микросервисной архитектуры** и **EDA (Event-Driven Architecture)**.  
Проект ориентирован на расширяемость, надёжность и простоту интеграции.

---

## 🎯 Цели сервиса

- Получать данные о корзине и адресе доставки.
- Параллельно опрашивать всех подключённых провайдеров.
- Возвращать агрегированный список вариантов доставки.
- Деградировать частично (падение одного провайдера ≠ ошибка всего запроса).
- Быть независимым и легко подключаемым микросервисом.
- Позволять подключать новых провайдеров без изменения существующего кода.

---

# 1. API сервиса

Сервис предоставляет HTTP-эндпоинт:

## **POST `/v1/shipping/options`**

### Пример запроса

\`\`\`json
{
"cart": {
"weightKg": 2.5,
"dimensionsCm": {
"length": 30,
"width": 20,
"height": 15
},
"totalPrice": 3500
},
"destination": {
"country": "RU",
"city": "Москва",
"postalCode": "101000"
},
"currency": "RUB"
}
\`\`\`

### Пример ответа

\`\`\`json
{
"options": [
{
"providerCode": "mock-cdek",
"serviceName": "Mock CDEK Standard",
"deliveryType": "courier",
"price": 300,
"currency": "RUB",
"estimatedDays": { "min": 2, "max": 4 }
},
{
"providerCode": "mock-boxberry",
"serviceName": "Mock Boxberry City Tariff",
"deliveryType": "pickup",
"price": 300,
"currency": "RUB",
"estimatedDays": { "min": 3, "max": 6 }
}
],
"providerErrors": [
{
"providerCode": "mock-boxberry",
"message": "Destination city is not supported"
}
]
}
\`\`\`

---

# 2. Внутренняя архитектура

## Архитектурный стиль

- **NestJS** + строгая модульность.
- Паттерны **Strategy** (каждый провайдер — отдельная стратегия).
- Паттерн **Registry** (единый список провайдеров).
- **EDA** через RabbitMQ.
- Разделение на слои: Delivery, Providers, Events, Infra, Shared.

---

## Структура проекта

\`\`\`
src/
main.ts
app.module.ts

delivery/
delivery.module.ts
api.controller.ts
core.service.ts

providers/
providers.module.ts
mock-cdek.service.ts
mock-boxberry.service.ts

events/
events.module.ts
publisher.service.ts
subscriber.controller.ts

infra/
infra.module.ts
rmq.service.ts

shared/
shared.module.ts
dto/
types/
\`\`\`

---

# 3. Расширяемость: добавление нового провайдера

Единый интерфейс:

\`\`\`ts
export interface ShippingProvider {
readonly code: string;
calculateOptions(req: CalculateDeliveryRequest): Promise<DeliveryOption[]>;
}
\`\`\`

Как добавить нового провайдера:

1. Создать файл `providers/<имя>.service.ts`.
2. Реализовать интерфейс `ShippingProvider`.
3. Добавить новый провайдер в массив, возвращаемый фабрикой `ProvidersModule`.

**Доменный сервис и контроллер менять не нужно.**

---

# 4. Надёжность и отказоустойчивость

### ✔ Параллельный опрос всех провайдеров
Через Promise.all.

### ✔ Таймаут на каждый провайдер
Если провайдер «висит», возвращается ошибка *только по нему*.

### ✔ Частичная деградация
Остальные провайдеры продолжают работать.

### ✔ Изоляция ошибок
Любое исключение → `providerErrors[]`.

### ✔ Расширяемость под Circuit Breaker
Структура позволяет внедрить CB без изменения публичного API.

---

# 5. Event-Driven Architecture (EDA)

Сервис работает как гибрид:

- обычный NestJS HTTP-сервер,
- и NestJS Microservice Transport (RabbitMQ).

## Событие: `delivery.options.requested`

\`\`\`json
{
"orderId": "123",
"cart": { "weightKg": 2 },
"destination": { "city": "Москва", "country": "RU" }
}
\`\`\`

## Событие: `delivery.options.calculated`

\`\`\`json
{
"orderId": "123",
"options": [
{ "providerCode": "mock-cdek", "price": 300 }
]
}
\`\`\`

---

# 6. Mock-провайдеры

## MockCdekProvider
- Формула: `ceil(weightKg) * 100`.

## MockBoxberryProvider
- Фиксированные тарифы по городам:

| Город              | Цена |
|--------------------|------|
| Москва             | 300  |
| Казань             | 500  |
| Санкт-Петербург    | 400  |

---

# 7. Unit-тесты

Покрывают:

- расчёт стоимости по весу (MockCdekProvider),
- логика выбора города (MockBoxberryProvider).

Запуск:

\`\`\`bash
npm test
\`\`\`

---

# 8. Docker и docker-compose

Сервис полностью контейнеризирован.

## Запуск

\`\`\`bash
docker-compose up --build
\`\`\`

## После старта доступны:

- Swagger — http://localhost:3000/docs
- HTTP API — http://localhost:3000/v1/shipping/options
- RabbitMQ UI — http://localhost:15672 (guest / guest)

## Пример проверки API:

\`\`\`bash
curl -X POST http://localhost:3000/v1/shipping/options \
-H "Content-Type: application/json" \
-d '{
"cart": { "weightKg": 2.5 },
"destination": { "country": "RU", "city": "Москва" }
}'
\`\`\`

---

# 9. Соответствие бизнес-целям Citadello

- ✔ Простое подключение новых служб доставки
- ✔ Масштабируемый сервис на EDA
- ✔ Отказоустойчивость и частичная деградация
- ✔ Наблюдаемость, прозрачность, изоляция ответственности
- ✔ Лёгкая поддержка и расширяемость

---

# 10. Планы развития 

- Подключение реальных провайдеров (CDEK, Boxberry, ПР, Яндекс).
- Хранение тарифов в БД; конфигурация провайдеров в админке.
- Circuit Breaker, retry, rate limiting.
- Кэширование тарифов.
- Метрики (Prometheus), трейсинг (OpenTelemetry).
- Авторизация (API-key или JWT).
- Мульти-тенантность: разные провайдеры для разных магазинов.

---


## Часть 2. План действий (В идеале)

### Приоритеты
1. **Оплата — Приоритет 1.** Прямые потери денег, решаю в первую очередь.
2. **Разработчик — Приоритет 2.** Он заблокирован, но можно дать ему работу.
3. **Баннер — Приоритет 2 (отправляю разработчика, что в простое).** Важно, но не критично прямо сейчас.

---

## План на 3–4 часа

### 1. Быстрая коммуникация (5–10 минут)

**CEO:**

> Привет! У нас ошибка в оплатах — теряем деньги. Беру в приоритет.  
> Так же есть, проблема с разработчиком, у него не разворачивается стенд,
> пока направлю его на верстку баннера, чтобы не простаивал, как только решу проблему с оплатой, помогу ему разблокироваться по стенду.


**Разработчику:**

> Я иду чинить оплату.  
> Ты пока возьми верстку баннера (ссылка на дизайн), чтобы время не терять.  
> Позже помогу с CI/CD.

---

## 2. Исправляю проблему с оплатой (примерно час)

- Проверяю ошибки в Sentry(если есть) и логи.
- Если проблема в последнем релизе — откатываю.
- Если в коде — делаю быстрый hotfix.
- Если внешняя проблема — запускаю временное решение.
- Проверяю, что ошибки уходят.

Сообщаю CEO: ошибки падают, ситуация стабилизируется.

---

## 3. Помогаю разработчику с CI/CD (20–30 минут)

- Быстро ищу проблему в конфиге или пайплайне.
- Если чинится быстро — починил.
- Если нет — даю обход (локальный запуск), чтобы он продолжил работать.

---

## 4. Завершаем баннер (оставшееся время)

- Смотрю, что разработчик сверстал.
- Делаю интеграцию, подключаю таймер, выкатываю.
- Проверяю на проде.

Сообщаю CEO:

> Баннер выкатили, всё работает, готово к запуску.

---

## Итог

За 3–4 часа:

- Починил критичную оплату.
- Разработчик не простаивал — сделал верстку баннера.
- CI/CD разблокирован или есть обход.
- Баннер выкатили вовремя.

## Часть 3

### ⭐ Технические метрики
Показывают стабильность и качество работы сервиса.

1. **Успешность запросов** — % успешных ответов (SLI)  
   Целевое значение: ≥ 99% успешных ответов (SLO).

2. **Время ответа сервиса** — среднее + p95/p99 (SLI)  
   Цель: p95 менее 300–500 мс (SLO).

3. **Количество ошибок 4xx/5xx** (SLI)  
   KPI: резкий рост — триггер на инцидент (SLO → SLA).

4. **Частота фолбеков/таймаутов к внешним провайдерам** (SLI)  
   Цель: доля фолбеков < 3% (SLO).

**Инструменты:** Sentry, Prometheus/Grafana, Kibana.

---

### ⭐ Бизнесовые метрики
Показывают, даёт ли фича реальную пользу.

1. **Конверсия “корзина → оплата”** 
   Если выросла — релиз дал ценность.

2. **Выбор методов доставки** — популярность вариантов 
   Понимаем, что предпочитают пользователи.

3. **Отказы после шага доставки** — churn на этапе checkout 
   Если снижается → фича помогает.

4. **Средняя стоимость заказа (AOV)**  
   Влияние доставки на чек.

5. **MRR / GMV, связанный с заказами с доставкой** 
   Рост этих показателей означает, что доставка повышает оборот.

---

### Короткий итог
Релиз успешен, если:
- техническое качество стабильно (SLI/SLO внутри норм),
- нет инцидентов (SLA не нарушено),
- растёт конверсия,
- падают отказы на этапе доставки,
- GMV/MRR от заказов с доставкой увеличиваются.

То есть оцениваю и качество сервиса, и реальную ценность для бизнеса.
